Add a new feature to the task manager in index.html.

The user will describe the feature. Before implementing:
1. Read index.html to understand the current structure
2. Confirm the feature scope in one sentence — ask if unclear
3. Implement only what was asked: no extra buttons, classes, or abstractions
4. Keep all code inside index.html (no separate files)
5. Follow the existing style: vanilla JS functions, green #2d6a4f accent, Flexbox layout

The feature to add: $ARGUMENTS
