Fix the 3 intentional bugs in index.html.

For each bug:
1. Read the current state of the file
2. Apply the fix
3. Explain in one line what was wrong and what you changed

The bugs are documented in CLAUDE.md. Fix them in order (CSS layout → JS getElementById → JS pending count).
After all 3 fixes, summarize the changes in a table: Bug | Line | Was | Now.
